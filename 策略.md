# AiToEarn — ETF 板块轮动策略完整说明

## 1. 策略概述

**核心思想**：在 A 股 ETF 中，通过"动量+趋势"双因子评分，每周选出表现最强的板块，持有其代表 ETF，实现板块轮动。

**策略类型**：中短期动量趋势跟踪（周频换仓）

**适用场景**：A股结构性行情，板块分化明显时效果最佳

---

## 2. 策略逻辑

### 2.1 评分因子

每只 ETF 通过两个因子计算综合评分：

| 因子 | 计算方式 | 含义 |
|------|----------|------|
| 20日涨跌幅 (w1) | `(当日收盘价 / 21日前收盘价 - 1) × 100` | 近期动量强度 |
| 20日上涨天数 (w2) | 近20个交易日中 `收盘价 > 开盘价` 的天数 | 趋势持续性 |

### 2.2 三套方案

不同风险偏好对应不同的因子权重：

| 方案 | 涨跌幅权重 | 上涨天数权重 | 风格 |
|------|-----------|-------------|------|
| A（激进） | 0.7 | 0.3 | 重动量，追涨 |
| B（平衡） | 0.6 | 0.4 | 动量+趋势均衡 |
| C（稳健） | 0.5 | 0.5 | 动量+趋势各半 |

**评分公式**：`score = w1 × 20日涨跌幅 + w2 × 20日上涨天数`

### 2.3 MA60 过滤

- 若 ETF 的 MA60 数据可用（上市满60个交易日），则要求**当前价 > MA60** 才参与评分
- 若 MA60 不可用（新上市 ETF），**不跳过**，仍参与评分
- 这样避免因 MA60 延迟导致回测起始日过晚

### 2.4 板块去重

- 每只 ETF 按名称关键词归类到14个板块（医药、科创、半导体、消费、新能源等）
- 每个板块**只保留评分最高的 1 只** ETF
- 防止同一板块多只 ETF 同时入选，集中度过高

---

## 3. V1 回测（原始策略）

### 3.1 参数

| 参数 | 值 |
|------|-----|
| 持仓数量 | Top 8 |
| 换仓频率 | 每周（周五评分，下周一执行） |
| 仓位分配 | 等权（每只 1/8） |
| 交易成本 | 0.05%（买卖各） |
| 净值基准 | 1.0 |
| 基准对比 | 沪深300（510300）买入持有 |

### 3.2 换仓逻辑

1. 每周五收盘后，对全部 ETF 按当前方案评分
2. 板块去重后取 Top 8
3. 下周一以开盘价执行换仓（卖出旧持仓，买入新 Top 8）
4. 每日按收盘价计算净值

### 3.3 统计指标

- 累计收益率
- 年化收益率
- 夏普比率（无风险利率 3%）
- 最大回撤
- 胜率（净值上涨天数占比）
- 换手率（每次换仓更换的 ETF 数量）
- 末段入场率（入选时上涨天数 < 8 的比例）

---

## 4. V2 回测（30万本金实盘模拟）

### 4.1 参数

| 参数 | 值 |
|------|-----|
| 初始本金 | ¥300,000 |
| 持仓数量 | Top 3 |
| 换仓频率 | 每周最后一个交易日收盘价执行 |
| 仓位分配 | 等权（总资产 ÷ 3，随净值滚动） |
| 交易成本 | 0（简化） |
| 回测起始 | 本年度第一个交易日 |
| 基准对比 | 30万买入沪深300（510300）持有不动 |

### 4.2 换仓逻辑

1. 每个换仓日收盘后，评分选股（板块去重 Top 3）
2. **先卖后买**：不在新 Top 3 的持仓全部卖出
3. 在 Top 3 中的 ETF：
   - 已在持仓中 → 调整仓位至目标份额（hold）
   - 不在持仓中 → 买入（buy）
4. 仓位计算：`目标份额 = 当前账户总资产 ÷ 3 ÷ 当日收盘价`

### 4.3 换仓日期算法

- 按 ISO 周日历分组，每周最后一个交易日为换仓日
- 节假日自动顺延（取当周最后一个有效交易日）
- 前端显示：周一~周四/周五收盘前显示本周五，周五收盘后/周六/周日显示下周五

---

## 5. 数据架构

### 5.1 数据源

| 数据源 | 用途 | 说明 |
|--------|------|------|
| akshare | 主数据源 | ETF 行情、历史 K 线、实时快照 |
| baostock | 副数据源 | 交叉校验收盘价，抽查 10 只 ETF |

### 5.2 数据流

```
akshare API
  ↓
ETF 快照（全市场）→ 过滤（去债/去跨境/规模/流动性/去重）→ 100只标的池
  ↓
逐只拉取历史 K 线 → 增量更新到 data/*.csv
  ↓
数据质量校验（完整性/涨跌幅/跳空） + 双源交叉校验
  ↓
评分计算 → db/score_history
  ↓
V1 回测 → db/equity_curve
V2 回测 → db/equity_curve_v2 + db/trade_history
  ↓
Flask Web API → 前端仪表盘
```

### 5.3 ETF 标的池筛选

1. 剔除债券/货币 ETF（关键词：债、货币、国债等）
2. 剔除跨境 ETF（关键词：美国、纳指、恒生、黄金、石油等）
3. 最低规模过滤：总市值 ≥ 5 亿
4. 最低流动性：日成交额 ≥ 3000 万
5. 同质化去重：同名核心词组只保留成交额最大的 1 只
6. 数量调节：截取成交量前 100 只

### 5.4 板块分类

```python
半导体: 半导体、芯片
科创: 科创、科创板
创业: 创业板
医药: 医药、医疗、生物、疫苗、中药、创新药
消费: 消费、食品、饮料、酒、家电
科技: 科技、TMT、通信、5G、电子、计算机、软件
新能源: 新能源、光伏、电池、锂电、储能、碳中和
军工: 军工、国防、航空
金融: 金融、银行、证券、保险、地产
煤炭: 煤炭、钢铁、有色、矿业、资源、化工
红利: 红利、股息、高息、价值
基建: 基建、建材、建筑、电力、公用
农业: 农业、畜牧、养殖、粮食
传媒: 传媒、游戏、影视
汽车: 汽车、智能车、新能车、新能源车
```

---

## 6. 数据库设计

### 6.1 核心表

| 表名 | 用途 | 主键 |
|------|------|------|
| score_history | 每日评分历史（144只ETF × 3方案） | (date, session, code) |
| equity_curve | V1 回测净值序列 | (date, scheme) |
| equity_curve_v2 | V2 回测净值（30万本金） | (date, scheme) |
| trade_history | V2 换仓记录（buy/sell/hold） | id |
| data_quality_log | 数据质量日志 | id |

### 6.2 score_history 字段

| 字段 | 类型 | 说明 |
|------|------|------|
| date | TEXT | 评分日期 |
| session | TEXT | intraday / final |
| code | TEXT | ETF 代码 |
| name | TEXT | ETF 名称 |
| sector | TEXT | 板块分类 |
| gain_20d | REAL | 20日涨跌幅(%) |
| up_days_20d | INTEGER | 20日上涨天数 |
| score_a/b/c | REAL | 三方案评分 |
| above_ma60 | INTEGER | 是否高于MA60 |
| rank_a/b/c | INTEGER | 三方案排名 |

---

## 7. Web 仪表盘

### 7.1 页面布局（6 区域）

```
┌─────────────────────────────────────────────┐
│ 区域1: 导航栏 — 方案切换(A/B/C) + 刷新按钮  │
├─────────────────────────────────────────────┤
│ 区域2: ETF 评分排行 — 全部144只，金银铜高亮  │
├─────────────────────────────────────────────┤
│ 区域3: 本周操作指令 — 当前持仓 + 换仓信息    │
├─────────────────────────────────────────────┤
│ 区域4: 收益曲线 — Plotly 交互图表            │
├─────────────────────────────────────────────┤
│ 区域5: 账户总览 — 总资产/累计收益/回撤/夏普  │
├─────────────────────────────────────────────┤
│ 区域6: 换仓历史 + 策略统计指标               │
└─────────────────────────────────────────────┘
```

### 7.2 API 接口

| 路径 | 说明 | 参数 |
|------|------|------|
| GET / | 仪表盘主页 | - |
| GET /api/ranking | ETF 评分排行 | scheme, session, limit |
| GET /api/equity/chart | V1 Plotly 图表 | - |
| GET /api/equity/v2 | V2 收益曲线+统计 | scheme, range(ytd/3m/all) |
| GET /api/trade/current | 当前持仓+换仓信息 | scheme |
| GET /api/trade/next | 下次换仓日期 | - |
| GET /api/trade/history | 换仓历史（按日期分组） | scheme, limit |
| POST /api/refresh | 手动刷新数据 | - |

### 7.3 方案切换联动

前端切换方案（A/B/C）时，以下区域全部联动刷新：
- ETF 排行（评分和排名按方案变化）
- 操作指令（持仓和换仓信息按方案变化）
- 收益曲线（净值曲线按方案变化）
- 账户总览（统计指标按方案变化）
- 换仓历史（按方案筛选）
- 策略统计（按方案计算）

---

## 8. 增量更新流程

### 8.1 定时调度

- **14:30**：盘中更新（session=intraday）
- **15:30**：收盘后更新（session=final，含完整回测）
- 非交易日自动跳过

### 8.2 更新步骤

```
1. 获取 ETF 标的池（最新快照 + 过滤）
2. 增量拉取 K 线数据
   - 无缓存：全量拉取 180 天
   - 有缓存：只拉取 last_date ~ today
   - 多轮重试：目标成功率 95%，最多 3 轮
3. 数据质量校验
   - 完整性/涨跌幅/跳空异常检测
   - 双源交叉校验（抽查 10 只）
4. 评分计算 + 回测更新
   - 144 只 ETF 评分 → score_history
   - V1 回测 → equity_curve
   - V2 回测 → equity_curve_v2 + trade_history
```

### 8.3 重试机制

- 单只 ETF 内部重试：指数退避，最多 5 次（base=2，即 1s/3s/7s/15s/31s）
- 批量多轮重试：第 1 轮全部遍历 → 成功率 < 95% 则等 5~10s 后只重试失败的 → 最多 3 轮
- 失败后等待加随机 jitter（1~2s），避免同时重试触发限流

---

## 9. 技术栈

| 层级 | 技术 |
|------|------|
| 语言 | Python 3.9+ |
| Web 框架 | Flask 3.x |
| 数据处理 | pandas, numpy |
| 数据源 | akshare, baostock |
| 数据库 | SQLite |
| 前端 | Bootstrap 5, Plotly.js 3.0, 原生 JS |
| 任务调度 | schedule 库（Flask 线程内） |
| 部署 | Docker + docker-compose |

---

## 10. 关键决策记录

1. **双因子评分**：20日涨跌幅（动量）+ 20日上涨天数（趋势），比单一动量因子更稳健
2. **板块去重**：防止同一板块多只 ETF 集中持仓，分散风险
3. **MA60 宽松过滤**：MA60 不可用时不过滤，避免回测起始日过晚
4. **V2 先卖后买**：换仓日先清掉不在 Top 3 的持仓，再买入新标的
5. **双源校验**：akshare 主数据 + baostock 抽查交叉校验，不强制全量
6. **增量更新**：只拉取增量 K 线，合并到已有缓存，减少 API 调用
7. **多轮重试**：批量更新 95% 成功率目标，3 轮重试，确保数据完整性